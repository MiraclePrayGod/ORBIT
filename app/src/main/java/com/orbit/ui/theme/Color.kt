package com.orbit.ui.theme

import androidx.compose.ui.graphics.Color

// ===== PALETA DE COLORES PROFESIONAL =====

// Colores Primarios - Sistema de colores coherente
val PrimaryBlue = Color(0xFF007AFF)        // Azul iOS profesional
val PrimaryBlueLight = Color(0xFF4A9EFF)   // Azul claro para hover
val PrimaryBlueDark = Color(0xFF0056CC)    // Azul oscuro para estados activos

// Colores Secundarios - Acentos profesionales
val SecondaryGreen = Color(0xFF34C759)     // Verde éxito Apple
val SecondaryOrange = Color(0xFFFF9500)    // Naranja advertencia Apple
val SecondaryRed = Color(0xFFFF3B30)       // Rojo error Apple
val SecondaryPurple = Color(0xFFAF52DE)    // Morado Apple
val SecondaryYellow = Color(0xFFFFCC00)    // Amarillo información

// Colores de Fondo - Jerarquía visual clara
val BackgroundPrimary = Color(0xFFFFFFFF)  // Blanco principal
val BackgroundSecondary = Color(0xFFF2F2F7) // Gris muy claro
val BackgroundTertiary = Color(0xFFE5E5EA)  // Gris claro
val BackgroundElevated = Color(0xFFFFFFFF)  // Blanco para elementos elevados

// Colores de Texto - Contraste optimizado
val TextPrimary = Color(0xFF1D1D1F)        // Negro principal
val TextSecondary = Color(0xFF8E8E93)      // Gris medio
val TextTertiary = Color(0xFFC7C7CC)       // Gris claro
val TextInverse = Color(0xFFFFFFFF)        // Blanco para fondos oscuros

// Colores de Estado - Semántica clara
val StatusSuccess = Color(0xFF34C759)      // Verde éxito
val StatusWarning = Color(0xFFFF9500)      // Naranja advertencia
val StatusError = Color(0xFFFF3B30)        // Rojo error
val StatusInfo = Color(0xFF007AFF)         // Azul información

// Colores de Pedidos - Estados específicos
val OrderInProgress = Color(0xFF007AFF)    // Azul en proceso
val OrderPaid = Color(0xFF34C759)          // Verde pagado
val OrderPending = Color(0xFFFF9500)       // Naranja pendiente
val OrderCancelled = Color(0xFFFF3B30)     // Rojo cancelado

// Colores de Stock - Indicadores de inventario
val StockAvailable = Color(0xFF34C759)     // Verde disponible
val StockLow = Color(0xFFFF9500)           // Naranja stock bajo
val StockOut = Color(0xFFFF3B30)           // Rojo sin stock

// Colores de Interfaz - Elementos UI
val BorderLight = Color(0xFFE5E5EA)        // Borde claro
val BorderMedium = Color(0xFFC7C7CC)       // Borde medio
val ShadowLight = Color(0x0A000000)        // Sombra sutil
val ShadowMedium = Color(0x1A000000)       // Sombra media
val Overlay = Color(0x80000000)            // Overlay oscuro

// Colores de Superficie - Material Design
val SurfacePrimary = Color(0xFFFFFFFF)     // Superficie principal
val SurfaceSecondary = Color(0xFFF2F2F7)   // Superficie secundaria
val SurfaceTertiary = Color(0xFFE5E5EA)    // Superficie terciaria

// Colores de Interacción - Estados de botones
val ButtonPrimary = Color(0xFF007AFF)      // Botón primario
val ButtonSecondary = Color(0xFFF2F2F7)    // Botón secundario
val ButtonDisabled = Color(0xFFC7C7CC)     // Botón deshabilitado

// Colores de Input - Campos de formulario
val InputBackground = Color(0xFFFFFFFF)    // Fondo de input
val InputBorder = Color(0xFFE5E5EA)        // Borde de input
val InputBorderFocused = Color(0xFF007AFF) // Borde enfocado
val InputError = Color(0xFFFF3B30)         // Borde de error

// Colores de Navegación - Elementos de navegación
val NavActive = Color(0xFF007AFF)          // Navegación activa
val NavInactive = Color(0xFF8E8E93)        // Navegación inactiva
val NavBackground = Color(0xFFFFFFFF)      // Fondo de navegación

// Colores de Notificación - Alertas y mensajes
val NotificationSuccess = Color(0xFF34C759) // Notificación éxito
val NotificationWarning = Color(0xFFFF9500) // Notificación advertencia
val NotificationError = Color(0xFFFF3B30)   // Notificación error
val NotificationInfo = Color(0xFF007AFF)    // Notificación información

// Colores legacy para compatibilidad
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
